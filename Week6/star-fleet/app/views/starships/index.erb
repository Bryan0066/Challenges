<h1>Ships</h1>

<ul>
  <% if @ships %>
    <% @ships.each do |ship| %>
      <li><a href= <%="/starships/#{ship.id}"%>>
        <%= ship.name %>
        </a>
      </li>
    <% end %>
  <% end %>

</ul>
  <button class="btn"> <a href="/starships/new">Click Here to Submit a Ship </a></button>
